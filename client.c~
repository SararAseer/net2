#include "pipe_networking.h"


int main() {
  char input[100];
  int to_server;
  int from_server;
  from_server = client_handshake( &to_server );
  while(1){
    printf("Send message?\n");
    fgets(input,100,stdin);
  }
  
}
